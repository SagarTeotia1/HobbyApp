import { StyleSheet } from 'react-native';
import { spacing } from '../../../../app/theme';

export const hobbyGridStyles = StyleSheet.create({
  grid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: spacing.sm,
  },
});
