import { StyleSheet } from 'react-native';
import { spacing } from '../../../../app/theme';

export const bossCharacterStyles = StyleSheet.create({
  root: {
    alignItems: 'center',
    paddingVertical: spacing.xl,
  },
  emoji: {
    fontSize: 80,
  },
});
